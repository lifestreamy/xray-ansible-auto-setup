---
- name: Restart xray
  ansible.builtin.systemd:
    name: xray
    state: restarted
    daemon_reload: true
  tags: config

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
  tags: service
